<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp">

    <TextView
        android:id="@+id/statusText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="En pause"
        android:textSize="18sp"
        android:layout_marginBottom="24dp" />

    <CheckBox
        android:id="@+id/autoCheckBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Mode automatique"
        android:layout_marginBottom="8dp" />

    <EditText
        android:id="@+id/intervalInput"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:inputType="number"
        android:hint="Intervalle (secondes)"
        android:text="5"
        android:layout_marginBottom="24dp" />

    <Button
        android:id="@+id/startButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Demarrer" />

    <Button
        android:id="@+id/stopButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Arreter"
        android:layout_marginTop="12dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Une bulle bleue apparait a l'ecran.\nAppuie dessus pour capturer.\nMaintiens et glisse pour la deplacer."
        android:textSize="13sp"
        android:gravity="center"
        android:layout_marginTop="24dp"
        android:alpha="0.7" />

</LinearLayout>
